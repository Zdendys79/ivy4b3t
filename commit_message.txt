feat: Move invasive action definition to database

- Add 'invasive' BOOLEAN column to action_definitions table
- Mark posting actions as invasive in database instead of config.json
- Update wheel logic to query database for invasive flag
- Remove affected_actions array from config.json
- Add SQL migration script for existing installations
- Improve async handling of invasive action checks

This centralizes invasive action configuration in the database where it belongs,
making it easier to manage and modify without touching config files.

🤖 Generated with [Claude Code](https://claude.ai/code)

Co-Authored-By: Claude <noreply@anthropic.com>