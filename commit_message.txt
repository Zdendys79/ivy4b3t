fix(analyzer): Oprava volání Node.js funkce z kontextu prohlížeče

Tato oprava řeší pád `fbSupport is not defined`, který byl způsoben nesprávným voláním Node.js funkce z `page.evaluate`.

- **refactor(analyzer): Oddělena extrakce dat a jejich zpracování**
  - Metoda `_performGroupAnalysis` byla přepsána tak, aby nejprve v `page.evaluate` pouze extrahovala syrový text a jednoduché příznaky ze stránky.
  - Následné zpracování těchto dat, včetně volání `fbSupport.findByText` pro nalezení tlačítka, se nyní provádí ve správném Node.js kontextu.
  - Tím je odstraněna příčina chyby a zajištěna správná funkčnost analýzy.