fix(db): Oprava chyby při volání surových SQL dotazů

Tato oprava řeší pád aplikace způsobený nesprávným voláním `safeQueryFirst` s přímým SQL textem místo názvu dotazu.

- **refactor(db): Vylepšena funkce `executeQuery` v `iv_sql.js`**
  - Funkce nyní inteligentně detekuje, zda je jí předán název dotazu (cesta) nebo surový SQL řetězec.
  - Díky tomu je možné bezpečně volat `safeQueryFirst` a `safeQueryAll` i s dynamicky generovanými dotazy, které nejsou v `queries/index.js`.
  - Tím se opravuje chyba `Query not found` a zároveň se zvyšuje flexibilita databázové vrstvy.