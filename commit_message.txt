refactor(worker): Sjednocení a oprava logiky pro mazání SingletonLock

Tato oprava vrací předchozí duplicitní kód a správně upravuje již existující logiku pro řešení problému se souborem `SingletonLock`.

- **revert(worker): Odstraněn duplicitní kód pro mazání zámku**
  - Zrušena předchozí změna, která zbytečně přidávala novou, téměř identickou logiku.

- **fix(worker): Opravena původní logika pro mazání `SingletonLock`**
  - Již existující `try...catch` blok byl upraven tak, aby nejprve ověřil existenci souboru `SingletonLock` pomocí `fs.existsSync()` a teprve poté se ho pokusil smazat.
  - Tímto se předchází zbytečným chybám a sjednocuje se chování na správný, robustní postup.