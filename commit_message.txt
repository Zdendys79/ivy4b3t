fix: Perform page analysis before checkpoint detection

- Change quickFBCheck to analyze page content first, then check for problems
- Only search for checkpoint/block causes if page analysis shows issues
- Fixes false checkpoint detection on functional pages
- Follows proper diagnostic flow: analyze → detect problems → find cause