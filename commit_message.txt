refactor: Remove unnecessary typeof db.* function checks and simplify code

- Remove all typeof db.function === 'function' checks - these are JS modules, not external APIs
- Simplify database calls by removing redundant fallbacks between modules
- Remove NaN check in calculateCharDelay - if it happens, it's a code bug to fix
- Make calculateCharDelay synchronous again since it no longer needs await
- Remove unreachable code and unnecessary fallback logic
- Improve code clarity by removing defensive programming where not needed

🤖 Generated with [Claude Code](https://claude.ai/code)

Co-Authored-By: Claude <noreply@anthropic.com>