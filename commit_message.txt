fix: Use startsWith match for "Napište něco" element detection

- Change matchType from 'exact' to 'startsWith' for "Napište něco" elements
- Elements contain "Napište něco" at the beginning but have additional text like user names
- Update both newThing() and handleSingleUtioPost to use startsWith matching
- Fixes element detection failure when text contains "Napište něco..." with suffix