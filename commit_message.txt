fix: Change text matching from startsWith to contains for post field detection

- Changed match strategy from 'startsWith' to 'contains' for both "Napište něco" and "Co se vám honí hlavou"
- Facebook elements often have whitespace or other text before the target string
- This should improve detection success rate for post creation fields