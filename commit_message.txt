feat(detection): Add new videoselfie error pattern

- Added the exact string "Potvrďte svou totožnost pomocí videoselfie" to the error detection patterns in `iv_page_analyzer.class.js`.
- This ensures that when this specific videoselfie request is detected on a page, the system correctly identifies it as a critical error.
- The existing error handling workflow will then lock the user account with the appropriate reason ("Požadavek na videoselfie") and lock type ("VIDEOSELFIE").
