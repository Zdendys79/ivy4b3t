revert: Revert accidental sql_config.json copy in test.sh

- Reverted the change in `test.sh` that copied `sql_config_example.json` to `sql_config.json`.
- The `sql_config.json` file is now expected to be created manually by the administrator during installation, as it contains sensitive data.