fix(analyzer): Oprava `await` ve funkci `page.evaluate`

Tato oprava řeší `SyntaxError: Unexpected reserved word`, který bránil spuštění aplikace.

- **fix(analyzer): Opravena definice `_performGroupAnalysis`**
  - Metoda byla označena jako `async`, což umožňuje správné použití `await` uvnitř ní.
- **refactor(analyzer): Přesunuta logika hledání tlačítka mimo `evaluate`**
  - Volání `fbSupport.findByText` bylo přesunuto mimo blok `page.evaluate`, protože `evaluate` běží v kontextu prohlížeče a nemá přístup k Node.js funkcím.
  - Tím je zajištěna správná funkčnost detekce tlačítka "Přidat se ke skupině".