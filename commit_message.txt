fix: Correct remaining import paths and add fallback for post field detection

- Fixed remaining iv_interactive_debugger.js import paths in iv_log.class.js
- Added fallback to newThing() method to search for "Co se vám honí hlavou" if "Napište něco" not found
- Reduced timeout for each search from 3s to 2s for faster fallback
- Maintains simple approach with only two text variants instead of complex strategies