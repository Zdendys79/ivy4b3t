fix: Focus element detection on DIV and SPAN with direct text

- Change selector to focus on div, span, a, button, input, label, h1-h6, p elements
- Return to using only direct text nodes (excluding child element text)
- Add deduplication to avoid showing same text multiple times
- Fix variable reference error (text -> directText)
- Better matches Facebook's UI where most clickable text is in DIV/SPAN