feat: Add debug incident logging test script and improve error logging

- Created `ivy/test_debug_incidents.js` to simulate various log messages and trigger debug report generation, including testing long messages for database column limits.
- Created `ivy/test.sh` as a wrapper to run the new test script.
- Further refined error logging in `ivy/iv_log.class.js` to ensure full SQL error details are captured and displayed.