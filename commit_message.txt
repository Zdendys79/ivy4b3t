feat: Remove PageAnalyzer cache completely for always-fresh results

- Remove all cache logic from PageAnalyzer (cacheResults, forceRefresh parameters)
- Remove cache-related parameters from all analyzeFullPage() calls
- Remove clearAnalysisCache() method from FBBot
- PageAnalyzer now always returns current page state without caching
- Simplify API by removing cache configuration options

Facebook pages are highly dynamic and cached results are never reliable.
This ensures all analysis results reflect the actual current page state.

🤖 Generated with [Claude Code](https://claude.ai/code)

Co-Authored-By: Claude <noreply@anthropic.com>