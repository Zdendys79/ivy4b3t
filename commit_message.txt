fix(ConsoleLogger): Sanitize log messages to prevent SQL errors

- All double quotes in captured console messages are now replaced with single quotes before being added to the log buffer.
- This prevents invalid SQL from being generated during batch log inserts, which was the root cause of the recurring `logs.insertConsoleLogBatch` query failures.
- This change should finally stabilize the console logging mechanism and prevent the debugger from being triggered by logging failures.