refactor(behavior): Oprava nadměrné chybovosti psaní úpravou dat

Tato oprava řeší problém s vysokou chybovostí při simulaci psaní správným, systémovým způsobem – úpravou výchozích dat místo "patchování" logiky.

- **revert(behavior): Odstraněno dělení chybovosti v logice**
  - Z metody `typeWord` byla odstraněna dočasná úprava, která dělila `mistake_rate` deseti.

- **refactor(behavior): Upravena výchozí chybovost pro nové profily**
  - V metodě `_createDefaultProfile` byla desetinásobně snížena generovaná hodnota `mistake_rate`, aby noví uživatelé měli od začátku realistickou chybovost.

- **fix(db): Provedena jednorázová oprava existujících profilů**
  - Byl spuštěn SQL skript, který všem existujícím záznamům v `user_behavioral_profiles` snížil hodnotu `mistake_rate` desetinásobně.