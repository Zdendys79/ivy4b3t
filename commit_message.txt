feat: Refresh element list after clicking in debugger

- Automatically refresh element list after each click (Enter key)
- Wait 2 seconds for page changes before refreshing
- Re-inject CSS styles in case page content changed
- Reset index if out of bounds after refresh
- Fix blinking animation by re-injecting styles