refactor(fb): Správná detekce a zpracování obrazovky souhlasu

Tato oprava vrací předchozí unáhlenou změnu a implementuje správný, modulární přístup ke zpracování obrazovky pro souhlas s reklamami.

- **revert(fb): Odstraněno přímé volání `resolveAdConsentFlow` z `openFB`**
  - Tím se zabraňuje zbytečnému spouštění této funkce na každé stránce.

- **feat(analyzer): Rozšířena detekce o `AD_CONSENT_REQUIRED`**
  - `PageAnalyzer` nyní dokáže specificky identifikovat obrazovku pro souhlas s reklamami a vrátit dedikovaný stav `ad_consent_required`.

- **refactor(fb): Implementována podmíněná logika v `openFB`**
  - Metoda `openFB` nyní nejprve spustí standardní analýzu.
  - Teprve pokud analýza vrátí stav `ad_consent_required`, zavolá se cílená metoda `resolveAdConsentFlow` pro vyřešení této konkrétní situace.
  - Tímto je zajištěn efektivní a správný postup v souladu s architekturou aplikace.