fix(actions): Oprava pádu po přechodu na stránku skupiny

Tato oprava řeší chybu `Cannot read properties of null (reading 'analyzeFullPage')`, která nastávala při provádění akcí `post_utio_g` a `post_utio_gv`.

- **fix(actions): Přidána reinicializace `PageAnalyzer` po `openGroup`**
  - V metodě `handleSingleUtioPost` je nyní po úspěšném přechodu na stránku skupiny explicitně volána metoda `fbBot.initializeAnalyzer()`.
  - Tím je zajištěno, že pro nově načtenou stránku vždy existuje platná instance analyzátoru, což zabraňuje pádu aplikace při následném pokusu o analýzu.