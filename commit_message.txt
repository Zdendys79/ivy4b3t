fix: Resolve logging issues and improve console output

- Addressed a bug where some logging functions (specifically in the Analyzer) were not awaited, leading to program execution chaos.
- Enhanced `ConsoleLogger` to remove all types of quotes (single, double, backticks) and backslashes from logged strings for cleaner output.
- Adjusted prefix parsing in `ConsoleLogger` to correctly extract and trim messages.
- Added `clear` command to `start.sh` for a cleaner console on script execution.