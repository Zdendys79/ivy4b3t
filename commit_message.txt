fix(Core): Stabilize logging and restore missing function

- fix(ConsoleLogger): Prevent recursive error logging by using `originalConsole.error` in the flush catch block. This stops the logger from trying to log its own failures, resolving the primary cause of debugger spam and stabilizing the application.
- fix(QueryBuilder): Re-introduce `db.verifyMsg` by creating an alias to the existing `db.verifyMessage` method. This restores functionality for code calling the legacy function name, resolving the `db.verifyMsg is not a function` error.
- refactor(SQL): The `verifyMsg` function has been restored in `iv_sql.js` to ensure it is available for direct calls if needed.