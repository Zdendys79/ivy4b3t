fix: Correct join button detection and prevent false checkpoint alerts

- Fix page analysis order: perform groupAnalysis before errorAnalysis
- Pass groupAnalysis results to _detectErrorPatterns for proper join button detection
- Improve join button detection to include SVG elements and aria-label attributes
- Increase image count threshold to include SVG elements (Facebook uses SVG instead of IMG)
- Correct isNormal threshold: elements>500, images>10, scripts>20, links>5
- Prevent false positive "security checkpoint" detection on normal Facebook group pages
- Use groupAnalysis.hasJoinButton in JOIN_GROUP_REQUIRED pattern detection

This fixes the issue where normal Facebook group pages requiring membership
were incorrectly flagged as security checkpoints instead of detecting the
join button properly.

🤖 Generated with [Claude Code](https://claude.ai/code)

Co-Authored-By: Claude <noreply@anthropic.com>