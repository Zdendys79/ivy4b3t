refactor: Rename database columns nazev->name and typ->type in fb_groups table

- Execute ALTER TABLE to rename nazev to name and typ to type
- Update all SQL queries to use new column names (name, type) 
- Create automated script to replace all code references (.nazev -> .name, .typ -> .type)
- Update 18 JavaScript files with new column references
- Fix web interface to use new column names
- Prepare for new group type 'E' (Exploration) for newly discovered groups
- Maintain backward compatibility with existing group types G, GV, P

🤖 Generated with [Claude Code](https://claude.ai/code)

Co-Authored-By: Claude <noreply@anthropic.com>