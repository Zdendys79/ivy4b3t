feat: Show only visible elements in viewport in debugger

- Add viewport visibility check for elements
- Only show elements currently visible on screen
- Check if element is within window boundaries (top, left, bottom, right)
- Helps focus on what user can actually see and interact with