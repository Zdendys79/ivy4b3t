refactor(analyzer): Zpřesnění detekce přihlašovací stránky

Tato oprava vylepšuje způsob, jakým `PageAnalyzer` detekuje neočekávané zobrazení přihlašovací stránky.

- **refactor(analyzer): Detekce přihlašovacího tlačítka podle textu**
  - Místo spoléhání na nestabilní `data-testid` atribut nyní analyzátor používá osvědčenou funkci `findByText` pro nalezení tlačítka s přesným textem "Přihlásit se".
  - Tím se zvyšuje spolehlivost detekce a snižuje riziko selhání při budoucích změnách designu Facebooku.