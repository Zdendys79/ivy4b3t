fix: Remove duplicate getUserWithAvailableActions method causing wrong SQL query

- Remove duplicate implementation at line 364 that was overriding the correct one
- Keep only the correct implementation at line 32 that calls users.getWithAvailableActions  
- Add test files for VM testing (test.js, test.sh)

This resolves the issue where users.getByHostname was called instead of 
users.getWithAvailableActions, causing wrong user selection logic.

🤖 Generated with [Claude Code](https://claude.ai/code)

Co-Authored-By: Claude <noreply@anthropic.com>