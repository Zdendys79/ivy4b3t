fix: Ensure full SQL error details are logged

- Modified `iv_log.class.js` to pass the original error object directly to `console.error`, allowing `ConsoleLogger` to capture and log complete SQL error messages, including database-specific details.