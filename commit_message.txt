feat: Add git status after commit in commit.sh

- Added `git status` command at the end of `commit.sh` to explicitly show the repository's clean state after a successful commit and push.
- This provides clear confirmation of the post-commit status, addressing user feedback.