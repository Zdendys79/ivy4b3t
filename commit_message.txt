fix(core): Oprava názvů importů v SQL modulech

Tato oprava řeší další `SyntaxError`, který bránil spuštění aplikace.

- **fix(db): Sjednocení názvů importovaných konstant v `sql/queries/index.js`**
  - Některé moduly (např. `action_quality.js`) exportovaly své konstanty v `camelCase`, zatímco `index.js` je očekával ve `UPPER_SNAKE_CASE`.
  - Všechny importy a jejich následné použití byly opraveny, aby odpovídaly skutečným názvům exportů.