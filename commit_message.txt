fix: Use discussion navigation only as last resort fallback

- Move discussion section navigation to be the final fallback strategy
- Only attempt "Diskuze" click when all other posting element search strategies fail
- Simplify clickDiscus() to just look for "Diskuze" text without over-complexity
- Revert isSellGroup() to simple detection to avoid false positives
- Retry posting element search after successful discussion navigation
- Conservative approach prevents unnecessary navigation in normal groups

This ensures posting elements are found in sell groups requiring discussion 
section while avoiding false positives in regular groups.

🤖 Generated with [Claude Code](https://claude.ai/code)

Co-Authored-By: Claude <noreply@anthropic.com>