refactor(analyzer): Zpřesnění detekce tlačítka "Přidat se ke skupině"

Tato oprava reaguje na upřesnění, že pro detekci tlačítka pro přidání do skupiny není potřeba hledat více variant textu.

- **refactor(analyzer): Zjednodušena detekce v `_performGroupAnalysis`**
  - Odstraněn široký seznam klíčových slov.
  - Detekce se nyní zaměřuje na přesnou shodu s textem "Přidat se ke skupině" v obsahu nebo v `aria-label` atributu.
  - Tím se zvyšuje přesnost a snižuje riziko falešně pozitivních nálezů.