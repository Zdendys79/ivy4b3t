fix: Ensure consistent data structure across PageAnalyzer consumers

- Fix ErrorReportBuilder to use navigation.elements instead of non-existent buttons
- Add bodyText to basic analysis for ErrorReportBuilder compatibility  
- Fix complexity analysis logging to read metrics.elements instead of elementCount
- Fix navigation analysis logging to read hasStandardNavigation instead of linkCount
- Add debug logging and diagnostic data (readyState, title) to complexity analysis
- Ensures all PageAnalyzer consumers expect the same data structure

Resolves inconsistencies where different parts of the codebase expected
different property names from the same analyzer methods.

🤖 Generated with [Claude Code](https://claude.ai/code)

Co-Authored-By: Claude <noreply@anthropic.com>