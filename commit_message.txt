fix(core): Oprava syntaktické chyby v FBBot

Tato oprava řeší `SyntaxError: Missing catch or finally after try`, který bránil spuštění aplikace.

- **fix(fb): Doplněn chybějící `catch` blok ve funkci `acceptCookies`**
  - V souboru `iv_fb.class.js` byl nalezen a opraven osamocený `try` blok, který způsoboval pád Node.js při parsování souboru.