fix(worker): Oprava chyby `path is not defined`

Tato oprava řeší `ReferenceError`, který způsoboval pád aplikace při pokusu o zjištění aktuální Git větve.

- **fix(support): Doplněn chybějící import modulu `path`**
  - Do souboru `iv_support.js` byl přidán nezbytný `import path from 'path';`.
  - Tím se opravuje chyba, která bránila správné funkci detekce větve a následné rotaci uživatelů v testovacím prostředí.