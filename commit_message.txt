feat: Add graceful browser shutdown and crash dialog handling

A) Auto-dismiss Chromium crash dialogs:
- Add _handleCrashDialogsAsync() method to automatically detect and close crash dialogs
- Implement _detectAndCloseCrashDialogs() with multiple selector strategies
- Run asynchronously with 5-second delay to not block main workflow
- Handle "Chromium didn't shut down correctly" and similar crash recovery dialogs

B) Graceful browser shutdown on Ctrl+C:
- Add global activeBrowsers Set to track all browser instances
- Implement shutdownAllBrowsers() function for coordinated browser cleanup
- Add SIGINT handler to ivy.js for proper Ctrl+C handling
- Ensure all pages and browser instances are closed before Node.js exit
- Add 10-second timeout protection for shutdown operations

🤖 Generated with [Claude Code](https://claude.ai/code)

Co-Authored-By: Claude <noreply@anthropic.com>