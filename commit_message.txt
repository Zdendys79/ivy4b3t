fix(fb): Oprava volání `findByText` v metodě `clickDiscus`

Tato oprava řeší chybu `this._findByText is not a function`, která nastávala při pokusu o nalezení tlačítka "Diskuze".

- **fix(fb): Aktualizováno volání v `clickDiscus`**
  - Během předchozího refaktoringu a centralizace funkce `findByText` bylo opomenuto aktualizovat její volání v metodě `clickDiscus`.
  - Volání bylo opraveno na `fbSupport.findByText`, čímž se obnovila správná funkčnost.
- **chore(fb): Doplněn chybějící import `iv_fb_support.js`**