fix(fb): Sjednocení a oprava logiky hledání tlačítek

Tato oprava řeší přetrvávající `TypeError: is not iterable` tím, že sjednocuje a zpřesňuje způsob, jakým jsou vyhledávány a zpracovávány klikatelné prvky.

- **refactor(fb): Zrobustněna metoda `_clickByText`**
  - Metoda byla přepsána tak, aby nepoužívala problematické "destructuring" přiřazení. Nyní nejprve zkontroluje, zda funkce `findByText` vrátila platné pole s prvky, a teprve poté se pokusí na prvek kliknout.

- **refactor(fb): `acceptCookies` nyní používá `findByText`**
  - Metoda pro odklikávání cookies byla přepsána, aby používala stejnou, nyní již opravenou a robustní, logiku jako `_clickByText`. Tím je zajištěna konzistence a spolehlivost.