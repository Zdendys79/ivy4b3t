revert: Odstranění svévolně přidaného UI příkazu

Tento commit vrací předchozí změny, které nesprávně a v rozporu se zavedenou architekturou přidávaly nový UI příkaz `LOGIN_USER`.

- **revert(ui): Odstraněn kód pro `LOGIN_USER` z `iv_ui.class.js`**
  - Veškerá logika spojená s tímto chybně navrženým příkazem byla odstraněna.
  - Systém se vrací k používání existujícího a správně fungujícího příkazu `call_user`, který zajišťuje požadované chování (přihlášení a čekání na interakci).

Tato změna opravuje mé předchozí chybné rozhodnutí a navrací systém do konzistentního stavu.