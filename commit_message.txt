fix: Improve element detection to find only clickable elements

- Add console.clear() at startup for cleaner output
- Filter out non-interactive tags (SCRIPT, STYLE, NOSCRIPT, etc.)
- Focus on clickable elements: links, buttons, inputs, and elements with click handlers
- Skip hidden elements (display: none, visibility: hidden, zero width)
- Use textContent instead of only direct text nodes to capture button/link text