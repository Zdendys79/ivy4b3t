feat: Add fallback element handling for "Diskuze" and "Přidat se ke skupině"

- Cache "Diskuze" and "Přidat se ke skupině" elements during analysis phase
- Use fallback elements only when "Napište něco" is not available
- Implement element validity checks with automatic fallback to fresh search
- Add discussion and join group element properties to FBBot constructor
- Optimize clickDiscus() and joinToGroup() methods to reuse cached elements
- Enhance verifyFBReadinessForUtio to find and cache all posting elements
- Implement intelligent fallback chain: direct -> discussion -> join group
- Prevents unnecessary element searches and improves posting reliability