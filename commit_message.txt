refactor: Remove unnecessary dryRun complexity from posting logic

- Remove dryRun parameter from clickElementWithText - was unnecessarily complex
- Simplify handleSingleUtioPost to directly click "Napište něco" instead of testing first
- Replace performSimplePublication with performDirectPublication (no duplicate clicking)
- Streamline logic: click once and proceed with publication if successful
- Eliminates redundant element testing and clicking phases