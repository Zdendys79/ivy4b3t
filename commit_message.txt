fix: Resolve logging and database connection issues

- Fixed `ReferenceError: logsToFlush is not defined` by moving its declaration.
- Implemented a `transaction` function in `iv_sql.js` for batch inserts.
- Updated `iv_console_logger.class.js` to use the new `transaction` function.
- Corrected `test.sh` to set `REPO_DIR` to the current working directory.
- Added `npm install` to `test.sh` to ensure dependencies are installed on the VM.
- Updated `sql_config_example.json` with correct database credentials and host.