feat: Implement simple in-memory invasive lock system

- Add single global invasive lock variable (timestamp only)
- Remove userId dependency - program works with one user at a time
- Initialize lock at start of user action cycle
- Set lock after successful invasive actions (4-8 min cooldown)
- Filter invasive actions from wheel of fortune during active lock
- Clear lock only at end of user session cleanup
- Remove unnecessary expired lock cleanup - expired locks have no effect
- Simplify logic: just compare current time with lock timestamp

This provides efficient posting cooldown without database overhead,
using simple timestamp comparison in memory.

🤖 Generated with [Claude Code](https://claude.ai/code)

Co-Authored-By: Claude <noreply@anthropic.com>