chore(debug): Přidání diagnostiky pro `invasive_lock`

Tato změna nepřináší opravu, ale přidává klíčové diagnostické logování pro odhalení příčiny, proč se nenastavuje časový zámek mezi invazivními akcemi.

- **chore(debug): Logování objektu akce v `runAction`**
  - Do `iv_actions.js` byl přidán log, který vypíše celý objekt `pickedAction` těsně před kontrolou, zda má být nastaven `invasive_lock`.
  - To nám umožní přesně zjistit, zda se vlastnost `invasive` správně předává z "kola štěstí" a proč podmínka pro nastavení zámku selhává.