refactor: Ensure async consistency in logging

- Marked `getActionRequirements` in `iv_actions.js` as `async` to properly handle the `await Log.warn` call.
- Updated all calls to `getActionRequirements` in `iv_actions.js` and `iv_worker.js` to use `await`.
- Marked the `disconnected` event handler in `iv_worker.js` as `async` to allow `await` for `Log.warn`.
- This change ensures that all functions that directly or indirectly call asynchronous logging functions are themselves asynchronous, preventing runtime errors.

🤖 Generated with Gemini
Co-Authored-By: Gemini <noreply@google.com>